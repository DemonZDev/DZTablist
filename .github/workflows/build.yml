name: Build Minecraft Plugin

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    name: Compile Plugin
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: maven

      - name: Install latest Maven
        run: |
          sudo apt-get update -y
          sudo apt-get install -y maven
          mvn -v

      - name: Unzip plugin source
        run: |
          unzip -o Test.zip -d ./plugin
          echo "✅ Unzipped Test.zip into ./plugin"
          ls -R ./plugin

      - name: Verify pom.xml
        run: |
          if [ ! -f "./plugin/pom.xml" ]; then
            echo "❌ pom.xml not found inside the unzipped directory!"
            exit 1
          fi

      - name: Build with Maven
        working-directory: ./plugin
        run: |
          echo "🚀 Starting Maven build..."
          mvn -B clean package | tee build.log
          BUILD_STATUS=${PIPESTATUS[0]}
          echo "----- Build Log (tail) -----"
          tail -n 40 build.log
          echo "----------------------------"
          if [ $BUILD_STATUS -ne 0 ]; then
            echo "❌ Build failed!"
            exit $BUILD_STATUS
          fi

      - name: Upload built jar (if successful)
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: built-plugin
          path: ./plugin/target/*.jar
